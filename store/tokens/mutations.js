export default {
  setList(state, list) {
    state.list = list;
  },

  setCurrent(state, current) {
    state.current = current;
  },

  setPrice(state, solPrice) {
    state.solPrice = solPrice;
  },
};
